// Smoke test — verifies the entry-point is importable.
// Full UI integration tests run on-device; see RELEASE_CHECKLIST.md.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    // TODO: Replace with real widget tests once CI environment has a simulator.
    expect(1 + 1, 2);
  });
}
